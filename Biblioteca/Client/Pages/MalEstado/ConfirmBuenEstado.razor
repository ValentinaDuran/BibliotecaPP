@page "/confirm-buenestado"
@using Biblioteca.BD.Data.Entidades
<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.h6">¿Desea volver al correcto funcionamiento?</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancelar">Cancelar</MudButton>
        <MudButton Color="Color.Primary" OnClick="Confirmar">Aceptar</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }
     [Parameter] public Inventario Inventario { get; set; }
    private void Cancelar()
    {
        MudDialog.Cancel();
    }

    private void Confirmar()
    {
        MudDialog.Close(DialogResult.Ok(true));
    }
}